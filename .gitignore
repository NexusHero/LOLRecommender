# Node
bridge/node_modules/
bridge/dist/
bridge/.env

# DDragon cache (wird beim ersten Start automatisch geladen, nicht committen)
bridge/.cache/

# Flutter
flutter_app/.dart_tool/
flutter_app/build/
flutter_app/.flutter-plugins
flutter_app/.flutter-plugins-dependencies
flutter_app/.packages

# General
.DS_Store
*.log

# Claude Code local session data (settings.local.json etc. bleiben lokal)
.claude/*
!.claude/commands/
!.claude/commands/**
